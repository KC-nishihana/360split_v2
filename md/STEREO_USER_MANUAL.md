# 360Split ステレオ機能ユーザーマニュアル

**バージョン**: 2.0
**最終更新**: 2026-02-12

---

## 📖 目次

1. [概要](#概要)
2. [ステレオ機能の使い方](#ステレオ機能の使い方)
3. [ステレオ表示モード](#ステレオ表示モード)
4. [GUI操作ガイド](#gui操作ガイド)
5. [CLI操作ガイド](#cli操作ガイド)
6. [よくある質問](#よくある質問)
7. [トラブルシューティング](#トラブルシューティング)

---

## 概要

360Split v2 では、OSV（Omnidirectional Stereo Video）ファイルに対応しました。
OSV ファイルは、VR や 3D 再構成用の左右2つのビデオストリームを含むステレオ動画フォーマットです。

### 主な機能

- ✅ **OSV ファイルの自動検出と読み込み**
- ✅ **5種類のステレオ表示モード**
  - Side-by-Side（左右並列表示）
  - アナグリフ（赤青メガネ用）
  - 左のみ表示
  - 右のみ表示
  - L/R 切り替え表示
- ✅ **ステレオペア出力**（`*_L.jpg` / `*_R.jpg`）
- ✅ **360度処理対応**（Equirectangular, Cubemap）
- ✅ **リアルタイムプレビュー**

---

## ステレオ機能の使い方

### 1. OSV ファイルの準備

OSV ファイルは、左右2つのビデオストリームを含むコンテナフォーマットです。

**OSV ファイルの作成例：**
```bash
# 左右の動画を2ストリームのコンテナに結合
ffmpeg -i left.mp4 -i right.mp4 \
  -map 0:v -map 1:v -c copy video.osv
```

**ストリーム確認：**
```bash
# OSV ファイルのストリーム情報を確認
ffprobe video.osv
```

### 2. 動作確認

**必須要件：**
- ffmpeg がインストールされていること

```bash
# ffmpeg インストール確認
which ffmpeg

# インストール（macOS）
brew install ffmpeg

# インストール（Ubuntu）
sudo apt install ffmpeg
```

---

## ステレオ表示モード

### 1. Side-by-Side（左右並列表示）

左右の映像を並べて表示します。ステレオ視差を確認するのに最適です。

**特徴：**
- 左右の映像が並列表示される
- 中央に緑の境界線が表示される
- 各映像に "LEFT" / "RIGHT" ラベルが表示される

**用途：**
- ステレオ視差の確認
- 左右のフレームの品質比較
- 同期確認

### 2. アナグリフ（赤青メガネ用）

赤青メガネで立体視できる形式で表示します。

**特徴：**
- 左目：赤チャンネル
- 右目：青・緑チャンネル
- グレースケール処理

**用途：**
- 赤青メガネを使った立体視
- 簡易的な3D確認

**注意：**
- 赤青メガネが必要です
- 色情報は失われます

### 3. 左のみ表示

左目ストリームのみを表示します。

**用途：**
- 左目フレームの詳細確認
- 通常の2D表示として使用

### 4. 右のみ表示

右目ストリームのみを表示します。

**用途：**
- 右目フレームの詳細確認
- 左右比較時の切り替え

### 5. L/R 切り替え表示

左右の映像を自動的に切り替えて表示します（500ms 間隔）。

**用途：**
- 視差の動的確認
- フリッカー効果による立体感の確認

---

## GUI操作ガイド

### ステップ1: OSV ファイルを開く

**方法A: ファイル選択ダイアログ**

1. メニューバー → **ファイル** → **開く** (Ctrl+O)
2. ファイルダイアログで `.osv` ファイルを選択
3. 「開く」をクリック

**方法B: ドラッグ＆ドロップ**

1. OSV ファイルをエクスプローラーから探す
2. 360Split のウィンドウにドラッグ＆ドロップ

### ステップ2: ステレオ表示モード選択

OSV ファイルを開くと、ビデオプレーヤーの下部に **「ステレオ:」** コンボボックスが表示されます。

1. コンボボックスをクリック
2. 表示モードを選択：
   - Side-by-Side
   - アナグリフ
   - 左のみ
   - 右のみ
   - L/R切替

選択すると即座に表示が更新されます。

### ステップ3: 再生とプレビュー

**基本操作：**
- **再生/停止**: `▶ 再生` ボタンまたはスペースキー
- **コマ送り**: `◀` / `▶` ボタンまたは左右矢印キー
- **シーク**: スライダーをドラッグまたはフレーム番号入力

**ステレオ表示中の操作：**
- 再生速度変更可能（0.25x ～ 4.0x）
- 360度パン（Equirectangular の場合）
- グリッドオーバーレイ（`Grid ON/OFF` ボタン）

### ステップ4: キーフレーム解析

1. メニューバー → **解析** → **フル解析 (Stage 1+2)** (Ctrl+R)
2. 解析中は進捗バーが表示される
3. 解析完了後、タイムラインにキーフレームマーカーが表示

**ステレオ解析の特徴：**
- 左右両方のフレームが品質評価される
- Conservative 方式：L/R 両方が基準を満たす必要がある
- 移動検出は Left のみで実行（コスト削減）

### ステップ5: エクスポート

1. メニューバー → **ファイル** → **キーフレームをエクスポート** (Ctrl+Shift+E)
2. エクスポートダイアログで設定：
   - 出力ディレクトリ
   - 画像フォーマット（PNG, JPG, TIFF）
   - JPEG 品質（JPG の場合）
   - 360度処理オプション
3. **エクスポート** ボタンをクリック

**出力結果：**
```
output/
├── keyframe_000001_L.jpg  # 左目
├── keyframe_000001_R.jpg  # 右目
├── keyframe_000050_L.jpg
├── keyframe_000050_R.jpg
└── keyframe_metadata.json
```

---

## CLI操作ガイド

### 基本コマンド

```bash
# OSV ファイルを解析してステレオペア出力
python main.py --cli video.osv --output output/
```

### プリセット指定

```bash
# 屋外プリセット（高品質重視）
python main.py --cli video.osv --preset outdoor --output output/

# 屋内プリセット（暗所対応）
python main.py --cli video.osv --preset indoor --output output/

# 混合プリセット（適応型）
python main.py --cli video.osv --preset mixed --output output/
```

### 360度処理との組み合わせ

```bash
# Equirectangular 変換 + マスク処理
python main.py --cli video.osv \
  --equirectangular \
  --apply-mask \
  --output output/

# Cubemap 出力
python main.py --cli video.osv \
  --equirectangular \
  --cubemap \
  --output output/
```

### 詳細オプション

```bash
# 最小フレーム間隔指定
python main.py --cli video.osv --min-interval 10 --output output/

# SSIM 閾値指定
python main.py --cli video.osv --ssim-threshold 0.7 --output output/

# 最大キーフレーム数制限
python main.py --cli video.osv --max-keyframes 100 --output output/

# 詳細ログ出力
python main.py --cli video.osv --verbose --output output/
```

---

## よくある質問

### Q1: OSV ファイルはどこで入手できますか？

**A:** OSV ファイルは、以下の方法で作成できます：

1. **2つの動画から作成：**
   ```bash
   ffmpeg -i left.mp4 -i right.mp4 -map 0:v -map 1:v -c copy video.osv
   ```

2. **ステレオカメラで撮影：**
   - Insta360 EVO
   - Vuze XR
   - その他のステレオ360度カメラ

### Q2: ステレオ表示が正しく動作しません

**A:** 以下を確認してください：

1. **ffmpeg がインストールされているか**
   ```bash
   which ffmpeg
   ```

2. **OSV ファイルが正しいか**
   ```bash
   ffprobe video.osv
   # ストリームが2つあることを確認
   ```

3. **ログを確認**
   - GUI: `logs/360split.log`
   - CLI: `--verbose` オプションで詳細ログ

### Q3: 左右のフレームが同期していない

**A:** OSV ファイルのストリームが同期していない可能性があります：

```bash
# 左右のフレーム数を確認
ffprobe -select_streams v:0 -count_frames video.osv
ffprobe -select_streams v:1 -count_frames video.osv
```

フレーム数が異なる場合は、元の動画を再エンコードしてください。

### Q4: メモリ不足エラーが発生します

**A:** 以下の対策を試してください：

1. **サンプリング間隔を広げる**
   ```bash
   python main.py --cli video.osv --min-interval 10
   ```

2. **一時ファイルを削除**
   ```bash
   rm -rf temp_streams/
   ```

3. **解像度を下げる**
   - 4K → 2K にダウンスケール

### Q5: アナグリフ表示が立体的に見えない

**A:** 以下を確認してください：

1. **赤青メガネを正しく装着**
   - 左目：赤
   - 右目：青

2. **画面距離を調整**
   - 画面から 50cm ～ 1m の距離

3. **視差が十分あるか確認**
   - Side-by-Side 表示で視差を確認

---

## トラブルシューティング

### エラー: "ffmpeg が見つかりません"

**原因:** ffmpeg がインストールされていない

**解決策:**
```bash
# macOS
brew install ffmpeg

# Ubuntu/Debian
sudo apt install ffmpeg

# Windows
# https://ffmpeg.org/download.html からダウンロード
```

### エラー: "ステレオストリームを開けません"

**原因:** OSV ファイルが破損しているか、ストリームが不足

**解決策:**
```bash
# ストリーム情報を確認
ffprobe video.osv

# ストリームが2つあることを確認
# Stream #0:0 - Left
# Stream #0:1 - Right
```

### エラー: "ステレオフレーム読み込み失敗"

**原因:** フレームインデックスがずれている

**解決策:**
1. OSV ファイルを再作成
2. 元の動画を確認
3. 別のフレームでテスト

### 表示が遅い

**原因:** 高解像度ビデオ、ディスク I/O

**解決策:**
1. 再生速度を下げる（0.5x）
2. プレビュー解像度を下げる
3. SSD を使用

### ステレオモードが表示されない

**原因:** 通常のビデオファイルを開いている

**解決策:**
- `.osv` ファイルを開いているか確認
- ファイル拡張子を確認
- OSV ファイルかどうか確認：
  ```bash
  ffprobe video.osv | grep "Stream"
  ```

---

## キーボードショートカット

### 一般

- **Ctrl+O**: ファイルを開く
- **Ctrl+R**: フル解析開始
- **Ctrl+Shift+E**: エクスポート
- **Ctrl+,**: 設定ダイアログ
- **Ctrl+Q**: 終了

### ビデオプレーヤー

- **Space**: 再生/停止
- **←/→**: コマ送り（前/次）
- **Home**: 先頭フレームへ
- **End**: 最終フレームへ

---

## パフォーマンスヒント

### 1. ストリーム分離の高速化

初回読み込み時に OSV ストリームが分離されます。分離後は `temp_streams/` にキャッシュされます。

```bash
# キャッシュ確認
ls -lh temp_streams/

# キャッシュクリア
rm -rf temp_streams/
```

### 2. メモリ使用量の最適化

- **フレームキャッシュ**: L/R 各100フレーム（LRU）
- **ピークメモリ**: 通常モード + 200MB 程度

### 3. ディスク使用量

- **一時ファイル**: OSV ファイルの約2倍のサイズ
- **出力**: キーフレーム数 × 2 × ファイルサイズ

---

## 高度な使い方

### カスタム設定ファイル

```bash
# 設定ファイルを作成
cat > my_settings.json << EOF
{
  "min_keyframe_interval": 15,
  "ssim_change_threshold": 0.75,
  "laplacian_threshold": 120.0,
  "motion_blur_threshold": 0.25
}
EOF

# 設定ファイルを使用
python main.py --cli video.osv --config my_settings.json --output output/
```

### バッチ処理

```bash
# 複数の OSV ファイルを一括処理
for osv in *.osv; do
  echo "Processing: $osv"
  python main.py --cli "$osv" --preset outdoor --output "output_${osv%.osv}/"
done
```

### COLMAP 連携

```bash
# キーフレーム抽出
python main.py --cli video.osv --output colmap_images/

# COLMAP で SfM
colmap feature_extractor \
  --database_path database.db \
  --image_path colmap_images/

colmap exhaustive_matcher \
  --database_path database.db

colmap mapper \
  --database_path database.db \
  --image_path colmap_images/ \
  --output_path sparse/
```

---

## サポート

問題が発生した場合は、以下の情報を含めて報告してください：

1. **動作環境**
   - OS とバージョン
   - Python バージョン
   - ffmpeg バージョン

2. **OSV ファイル情報**
   ```bash
   ffprobe video.osv
   ```

3. **エラーログ**
   - GUI: `logs/360split.log`
   - CLI: `--verbose` オプションで実行

4. **実行コマンド**
   - 実際に実行したコマンド

---

**作成日**: 2026-02-12
**バージョン**: 1.0.0
